import { LitElement, html, css } from 'lit-element';

export class BatteryStatusIcon extends LitElement {

    render() {
        return html``;
    }
}
customElements.define('battery-status-icon', BatteryStatusIcon);